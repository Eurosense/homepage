node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
.env
.env.local

# Raw Squarespace HTML snapshots are large and only needed to re-run the parser.
# The parsed content (content/) and downloaded assets (public/) ARE committed —
# they are the migration deliverable and must outlive the Squarespace account.
archive/raw/

# Local visual checks
.screenshots/
.playwright-mcp/

# qa-bot local knowledge graph
.qa-bot/

# Video re-encode markers (build state, not output)
archive/video-optimised/

# Copied from node_modules by scripts/vendor-dashboard-libs.mjs on every build.
public/dashboard-app/vendor/
